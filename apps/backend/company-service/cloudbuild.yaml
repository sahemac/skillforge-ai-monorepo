steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service:$COMMIT_SHA'
      - '-t'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service:latest'
      - '.'

  # Push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'company-service'
      - '--image'
      - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service:$COMMIT_SHA'
      - '--region'
      - 'europe-west1'
      - '--platform'
      - 'managed'
      - '--no-allow-unauthenticated'
      - '--set-env-vars'
      - 'ENVIRONMENT=production,USE_SECRET_MANAGER=true,GCP_PROJECT_ID=$PROJECT_ID,GCP_REGION=europe-west1,ENABLE_METRICS=true,LOG_LEVEL=INFO'
      - '--set-secrets'
      - 'DATABASE_URL=DATABASE_URL:latest,SECRET_KEY=SECRET_KEY:latest'
      - '--add-cloudsql-instances'
      - 'skillforge-ai-mvp-25:europe-west1:skillforge-pg-instance-staging'
      - '--service-account'
      - 'company-service@skillforge-ai-mvp-25.iam.gserviceaccount.com'
      - '--min-instances'
      - '1'
      - '--max-instances'
      - '10'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--cpu-boost'
      - '--timeout'
      - '600s'

images:
  - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service:$COMMIT_SHA'
  - 'europe-west1-docker.pkg.dev/$PROJECT_ID/skillforge-registry/company-service:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
